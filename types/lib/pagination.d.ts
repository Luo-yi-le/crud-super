import { InjectOptions, RenderElement, IMixins } from './options'

declare const _default: {
    mixins?: IMixins;
    inject?: InjectOptions;
    name?: string;

    methods: {
        currentChange(index: any): void;
        sizeChange(size: any): void;
    };
    render: RenderElement;
};
export default _default;
